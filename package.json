{
  "name": "sharebite-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "prisma:generate": "npx prisma generate --schema=prisma/schema.prisma",
    "prisma:push": "cd backend && npx prisma db push --schema=../prisma/schema.prisma",
    "seed": "cd backend && npx tsx src/seed.ts",
    "vercel-build": "npx prisma generate --schema=prisma/schema.prisma && cd frontend && npm run build && mkdir -p dist && cp -r ../dist/* dist/"
  }
}
